{
  "name": "ledger-co-root",
  "private": true,
  "scripts": {
    "dev": "npm --prefix ledger/ledger-frontend run dev",
    "build:frontend": "npm --prefix ledger/ledger-frontend run build",
    "preview:frontend": "npm --prefix ledger/ledger-frontend run preview",
    "lint:frontend": "npm --prefix ledger/ledger-frontend run lint"
  }
}
